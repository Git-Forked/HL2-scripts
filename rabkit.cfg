// Clear console
clear

echo "╔═ℴ♥ℴ══════════════════════════════════════════╗"
echo " RAB's 'Half-Life 2' & 'Source mod' kit: "
echo "╚══════════════════════════════════════════ℴ♥ℴ═╝"

// Load custom filters:
exec rab_filters.cfg

// Load custom binds:
exec rab_binds.cfg

// Load custom settings:
exec rab_settings.cfg

// Load last quick save game:
exec rab_quickload.cfg

// Load cheats:
// Off by default, instead use {o} key in game to enable cheats.
// Cheats are best run in solo player games, or servers that allow them.
// Uncomment this line below by removing the two "//" if you want cheats to always auto-load.
//exec rab_cheats.cfg
